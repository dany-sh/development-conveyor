# Human decision report

Project: `{project_id}`
Repository: `{repository}`
Run: `{run_id}`
Milestone: `{milestone}`
Feature: `{feature}`

Report only verified evidence. Identify the conflicting state, branches and commits involved, active Git operation, affected paths without reproducing sensitive contents, safe available actions, the recommended non-destructive action, and this exact resume command:

`scripts/conveyor resume --project {project_id}`

Do not choose a destructive recovery action. Do not merge, push, tag, publish, deploy, release, notarize, reset, clean, stash, or discard work.

